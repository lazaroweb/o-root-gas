{
  "name": "forja-public",
  "version": "1.0.0",
  "private": true,
  "description": "Formulário público de Discovery da Forja — projeto Apps Script SEPARADO e anônimo. Grava na mesma planilha (FORJA_SHEET_ID).",
  "scripts": {
    "build": "node esbuild.mjs",
    "push": "npm run build && npx clasp push --force",
    "test": "vitest run",
    "deploy": "node scripts/deploy.cjs"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.83",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "esbuild-wasm": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=22"
  }
}
